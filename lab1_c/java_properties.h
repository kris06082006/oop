#ifndef PROPERTIES_H
#define PROPERTIES_H

#include <stdio.h>
#include <stdlib.h>
#include <string.h>

char* array_to_properties(const char*, const char*);
char* properties_to_array(const char*);
#endif
